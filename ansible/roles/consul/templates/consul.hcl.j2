datacenter = "{{ datacenter }}"
data_dir   = "{{ consul_data_dir }}"
bind_addr  = "0.0.0.0"
advertise_addr = "{{ ansible_default_ipv4.address }}"
client_addr = "0.0.0.0"

server           = true
bootstrap_expect = 1

ui_config {
  enabled = true
}

log_level = "INFO"
